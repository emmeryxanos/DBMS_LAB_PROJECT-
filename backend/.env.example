SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=REPLACE_ME
RENDER_URL=https://your-backend.onrender.com
# Supabase project settings -> API -> JWT Secret (Legacy JWT Secret). Required to verify tokens server-side.
SUPABASE_JWT_SECRET=REPLACE_ME
# Comma-separated list of allowed frontend origins (add your deployed Vercel URL here)
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000
